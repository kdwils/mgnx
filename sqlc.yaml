version: "2"
sql:
  - engine: "postgresql"
    queries: "db/schema/queries/"
    schema: "db/schema/migrations/"
    gen:
      go:
        package: "gen"
        out: "db/gen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
